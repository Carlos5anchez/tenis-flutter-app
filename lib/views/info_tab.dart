import 'package:flutter/material.dart';

class InformacionTab extends StatelessWidget {
  const InformacionTab({super.key});
  /**TO-DO:
   * Crear vista de información
  **/
  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[
        Text("InfoBae"),
      ],
    );
  }
}
